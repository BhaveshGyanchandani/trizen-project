# Copy this file to .env.local and point it at your backend once it's deployed.
# Everything in the app reads this through lib/api.js — nothing else needs to change.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
