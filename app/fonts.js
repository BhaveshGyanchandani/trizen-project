// Self-hosted fonts via @fontsource — no runtime requests to Google Fonts,
// so this builds and renders identically with or without network access.
//
// One sans family across every surface (admin, team, customer gallery) —
// the previous design used a serif display face + monospace metadata
// labels for a "darkroom" register-switch effect; this direction is
// deliberately quieter and uses weight/size instead of typeface changes
// to carry hierarchy.
import "@fontsource/inter/400.css";
import "@fontsource/inter/500.css";
import "@fontsource/inter/600.css";
import "@fontsource/inter/700.css";
